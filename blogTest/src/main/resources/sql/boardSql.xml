<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
	<insert id="write" parameterType="hashmap">
		insert into board(bName, bTitle, message, member, bdate)
		values(#{bName}, #{bTitle}, #{message}, #{member}, now())
	</insert>
	<select id="lastList" resultType="hashmap">
		select *
		from board
		order by idx desc
	</select>
	<select id="view"  parameterType="String" resultType="hashmap">
		select *
		from board
		where idx = #{idx}
	</select>
	<update id="bHitUpdate" parameterType="String">
		update board set bHit = bHit+1 where idx =#{idx}
	</update>
</mapper>